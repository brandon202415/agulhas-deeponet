# Data — downloaded on the cluster, tens of GB; never commit
data/
*.nc

# Run outputs — regenerated by training
results/
logs/

# Eddy-tracking per-day detail dumps — regenerated from results/*/predictions.npz via
# eddy_tracking_analysis.py; individual files run ~300MB, over GitHub's 100MB hard limit
eddy_tracking/results_r3_std_mplfix_check/
eddy_tracking/results_r6_mplfix/
eddy_tracking/results_r3_aviso/
# stale pre-matplotlib-fix duplicate of results_r3_std_mplfix_check/, see RESEARCH_LOG.md
eddy_tracking/results_r3_std/

# Environments — macOS .venv won't run on Linux; conda envs live elsewhere
.venv/
__pycache__/
*.pyc

# Local tool config — machine/session-local, not project content
.claude/

# OS cruft
.DS_Store
